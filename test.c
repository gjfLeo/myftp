#include "cmd.h"
#include "file.h"
#include "header.h"

int main(int argc, char const *argv[])
{
    char line[MAX_LENGTH];
    char cmd[MAX_LENGTH];
    int a;
    gets(line);
    printf("asd\n");
}
